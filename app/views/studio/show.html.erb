<h1><%= @user.username %>'s Studio</h1>

<table>
  <% @studio.gear.each do |g| %>
    <tr>
      <td><%= g.name %></td>
    </tr>
  <% end %>
</table>

<% if @logged_in %>
  <%= link_to 'Add Gear', '/upload', :class => 'button' %>
<% end %>
